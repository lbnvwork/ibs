# Changelog

Формат версий — SemVer: `MAJOR.MINOR.PATCH` = итерация.спринт.деплой.
Источник истины — git-тег `v<MAJOR>.<MINOR>.<PATCH>` (см. `scripts/release.sh`).

## v0.3.1 — спринт 3 (старт версионирования)
- 3.83: введено версионирование (SemVer + git tag + отображение версии в сайдбаре).
