
## Описание

- **Спринт:** 2  
- **Код:** 2.15  
- **Оценка:** 1 день  
- **Функция:** FUNC-008 (Фармакогенетика)  
- **Статус:** К выполнению  

**Цель:**  
Привести блок фармакогенетики к единому формату «только отдельные SNP». Для гена CYP2C9 вместо интегрального диплотипа (*1/*1 и т.д.) вводятся два независимых маркера: CYP2C9*2 (rs1799853) и CYP2C9*3 (rs1057910). Удаляются поля «Дата теста» и «Комментарий». Для варфарина в интерфейсе остаются четыре выпадающих списка: CYP2C9*2, CYP2C9*3, VKORC1 G3673A, VKORC1 G3730A.

**Исходное состояние:**  
- Выполнена задача 2.14: созданы таблицы `genetic_markers`, `marker_drug_relations`, `patient_genetic_results`. В `patient_genetic_results` присутствуют поля `test_date` и `comment`.  
- Маркер `CYP2C9` (диплотип) существует и привязан к варфарину/фенилину.  
- В БД есть тестовые данные, которые можно безболезненно удалить.

**Требования к реализации:**

1. **Справочник genetic_markers**
   - Удалить запись с `gene_symbol = 'CYP2C9'` (интегральный диплотип).
   - Добавить две новые записи:

| gene_symbol | full_name | rs_id | possible_values |
|-------------|-----------|-------|-----------------|
| CYP2C9_2 | Цитохром P450 2C9, аллель *2 | rs1799853 | `CC` (норма), `CT` (гетерозигота, снижение активности), `TT` (мутантная гомозигота) |
| CYP2C9_3 | Цитохром P450 2C9, аллель *3 | rs1057910 | `AA` (норма), `AC` (гетерозигота, снижение активности), `CC` (мутантная гомозигота) |

   - Для VKORC1_3673 и VKORC1_3730 обновить `possible_values` и описания в соответствии с данными заказчика (если ещё не сделано).

2. **Связи с препаратами**
   - Удалить все связи старого маркера CYP2C9 с препаратами.
   - Создать связи для CYP2C9_2 и CYP2C9_3 с варфарином и фенилином (аналогично тому, как было у удалённого маркера).

3. **Удаление полей test_date и comment**
   - Создать миграцию, удаляющую столбцы `test_date` и `comment` из таблицы `patient_genetic_results`.
   - Обновить сущность `PatientGeneticResult`, убрать соответствующие свойства, скорректировать группы сериализации API Platform.
   - Удалить поля из ответа `GET /api/patients/{id}/pharmacogenetics`.
   - На фронтенде в `PharmacogeneticsSection.vue` убрать input'ы даты и комментария.

4. **Миграция данных (Вариант A)**
   - Так как ранее введённые данные являются тестовыми и не представляют ценности, применить жёсткий сценарий:
     - Удалить все записи из `patient_genetic_results`, связанные со старым маркером CYP2C9.
     - Удалить сам маркер.
   - Никакого преобразования диплотипов в SNP не производится.

5. **Интерфейс**
   - Компонент `PharmacogeneticsSection.vue`:
     - Для пациентов на варфарине теперь отображает ровно четыре выпадающих списка: CYP2C9*2, CYP2C9*3, VKORC1 G3673A, VKORC1 G3730A.
     - Списки формируются на основе `possible_values` из ответа API.
     - Первый пункт каждого списка — «Не исследовано» (соответствует `value = null`).
     - Поля даты и комментария отсутствуют.
   - Логика сохранения без изменений (сравнение с исходным значением, POST/PUT/DELETE). При выборе «Не исследовано» выполняется DELETE.

6. **API**
   - `GET /api/patients/{id}/pharmacogenetics` для варфарина возвращает четыре маркера с их `currentValue`. Старый CYP2C9 больше не возвращается.
   - Убедиться, что для апиксабана/ривароксабана возвращаются ABCB1 и CYP3A5, для дабигатрана – ABCB1 (если соответствующие связи были настроены в задаче 2.14).

7. **Обновление консольной команды**
   - Доработать `app:seed-genetic-markers` так, чтобы она создавала CYP2C9_2 и CYP2C9_3 вместо удалённого CYP2C9. Команда должна быть идемпотентной (повторный запуск не дублирует данные).

**Ожидаемый результат:**
- В карточке пациента на варфарине отображаются и редактируются четыре SNP-маркера.
- Поля «Дата теста» и «Комментарий» отсутствуют в БД, API и интерфейсе.
- Тестовые данные успешно удалены, старый маркер CYP2C9 полностью выведен из системы.
- Врач может внести генотипы по отдельным нуклеотидным заменами, без дублирующего диплотипа.

**Файлы для доработки:**
- Бэкенд: миграции, сущность `PatientGeneticResult`, `PharmacogeneticsController`, консольная команда, возможно `GeneticMarker` (если меняются возможные значения).
- Фронтенд: `PharmacogeneticsSection.vue`.

---

Если потребуется актуализировать тест-кейс под задачу 2.15, я могу его переделать отдельно. Как только утвердишь постановку, она отправится в сводную таблицу Спринта 2.

## Тесткейс

**Объект тестирования:** Компонент `PharmacogeneticsSection` в карточке пациента, API `/api/patients/{id}/pharmacogenetics`, таблицы БД `genetic_markers` и `patient_genetic_results`.

**Предусловия:**
- Выполнена миграция: старый маркер CYP2C9 удалён, добавлены CYP2C9_2 и CYP2C9_3, поля `test_date` и `comment` отсутствуют.
- Консольная команда `app:seed-genetic-markers` выполнена, справочники заполнены.
- В системе есть три пациента: один с активным лечением варфарином, второй – апиксабаном, третий – дабигатраном. У всех отсутствуют ранее сохранённые генетические результаты.
- Пользователь авторизован как врач.

---

### Сценарий 1. Отображение маркеров для пациента на варфарине

| Шаг | Действие | Ожидаемый результат |
|-----|----------|----------------------|
| 1.1 | Открыть карточку пациента с лечением варфарином. | В разделе «Фармакогенетика» отображаются четыре маркера: **CYP2C9\*2**, **CYP2C9\*3**, **VKORC1 G3673A**, **VKORC1 G3730A**. |
| 1.2 | Для каждого маркера посмотреть значение. | Для всех четырёх отображается «Не исследовано». |
| 1.3 | Проверить, что старый маркер «CYP2C9» отсутствует. | Никакого пятого поля или упоминания диплотипа нет. |
| 1.4 | Проверить отсутствие дат и комментариев. | Ни в одном маркере нет строк «Дата теста» или «Комментарий». |

---

### Сценарий 2. Ввод генотипов и сохранение

| Шаг | Действие | Ожидаемый результат |
|-----|----------|----------------------|
| 2.1 | Нажать «Редактировать» в блоке персональных данных (или в родительском разделе, если редактирование общее). | Все поля фармакогенетики становятся активными (выпадающие списки). |
| 2.2 | Для **CYP2C9\*2** выбрать `CT`. | В списке выбран пункт «CT (гетерозигота, снижение активности)». |
| 2.3 | Для **CYP2C9\*3** выбрать `AA`. | Выбран «AA (норма)». |
| 2.4 | Для **VKORC1 G3673A** выбрать `GA`. | Выбран «GA (гетерозигота, повышение чувствительности)». |
| 2.5 | Для **VKORC1 G3730A** оставить «Не исследовано». | Список в исходном состоянии. |
| 2.6 | Нажать «Сохранить» (вместе с сохранением персональных данных пациента). | Данные успешно сохранены. Режим редактирования закрыт. |
| 2.7 | Проверить отображение в режиме чтения. | **CYP2C9\*2** – `CT (гетерозигота, снижение активности)`, **CYP2C9\*3** – `AA (норма)`, **VKORC1 G3673A** – `GA (гетерозигота, повышение чувствительности)`, **VKORC1 G3730A** – «Не исследовано». |
| 2.8 | Обновить страницу (F5). | Значения не изменились. |

---

### Сценарий 3. Изменение и отмена

| Шаг | Действие | Ожидаемый результат |
|-----|----------|----------------------|
| 3.1 | Снова нажать «Редактировать». | Поля предзаполнены сохранёнными значениями из шага 2. |
| 3.2 | Изменить **CYP2C9\*2** с `CT` на `CC`. | Выбран «CC (норма)». |
| 3.3 | Нажать «Отмена». | Режим редактирования выключен. Отображается прежнее значение `CT`. |
| 3.4 | Убедиться, что другие поля не изменились. | **CYP2C9\*3** – `AA`, **VKORC1 G3673A** – `GA`, **VKORC1 G3730A** – «Не исследовано». |

---

### Сценарий 4. Сброс результата до «Не исследовано»

| Шаг | Действие | Ожидаемый результат |
|-----|----------|----------------------|
| 4.1 | Перейти в режим редактирования. | Поля активны. |
| 4.2 | Для **CYP2C9\*2** выбрать «Не исследовано». | |
| 4.3 | Для **CYP2C9\*3** также выбрать «Не исследовано». | |
| 4.4 | Нажать «Сохранить». | Сохранение успешно. |
| 4.5 | Проверить режим чтения. | Оба маркера показывают «Не исследовано». **VKORC1 G3673A** остался `GA`. |
| 4.6 | Обновить страницу. | Изменения сохранились. |

---

### Сценарий 5. Пациент на апиксабане

| Шаг | Действие | Ожидаемый результат |
|-----|----------|----------------------|
| 5.1 | Открыть карточку пациента с лечением апиксабаном. | В разделе «Фармакогенетика» отображаются **только ABCB1** и **CYP3A5**. |
| 5.2 | Убедиться, что маркеры варфарина отсутствуют. | Нет CYP2C9*2, CYP2C9*3, VKORC1 G3673A, VKORC1 G3730A. |
| 5.3 | Проверить варианты в выпадающих списках (перейти в редактирование). | **ABCB1**: `CC`, `CT`, `TT`. **CYP3A5**: `*1/*1`, `*1/*3`, `*3/*3`. |
| 5.4 | Ввести значения, сохранить, проверить отображение. | Данные корректно сохраняются. |

---

### Сценарий 6. Пациент на дабигатране

| Шаг | Действие | Ожидаемый результат |
|-----|----------|----------------------|
| 6.1 | Открыть карточку пациента с лечением дабигатраном. | Отображается **только ABCB1**. CYP3A5 отсутствует (если связи с дабигатраном нет). |
| 6.2 | Проверить, что можно выбрать генотип и сохранить. | Работает как ожидается. |

---

### Сценарий 7. Проверка API (через DevTools или Postman)

| Шаг | Действие | Ожидаемый результат |
|-----|----------|----------------------|
| 7.1 | Выполнить `GET /api/patients/{id}/pharmacogenetics` для пациента на варфарине. | В ответе массив из четырёх объектов. Поля `testDate` и `comment` отсутствуют в каждом объекте. `currentValue` соответствует сохранённым данным. |
| 7.2 | Выполнить запрос для пациента на апиксабане. | В ответе два маркера. |
| 7.3 | Попытаться через `POST /api/patient_genetic_results` создать запись с `value` не из `possibleValues` (например, `CC` для CYP2C9*3). | Сервер возвращает `422 Unprocessable Entity`. |

---

### Критерии успешного прохождения:
- Все сценарии выполняются без ошибок.
- В консоли браузера нет предупреждений или ошибок.
- Старый маркер CYP2C9 нигде не фигурирует.
- Поля «Дата теста» и «Комментарий» полностью исключены из системы.
- Количество отображаемых маркеров строго соответствует назначенному препарату.

Если понадобится, могу добавить проверку миграции на пустой БД (отсутствие старых записей) или стресс-тест с большим числом пациентов. Но основное покрытие здесь уже есть.

## Ход разработки
### План разработки

#### 1. Миграция базы данных
**Шаг 1 – удаление полей `test_date` и `comment` из `patient_genetic_results`**
**Шаг 2 – удаление старого маркера CYP2C9 и всех его связей/результатов**
**Шаг 3 – вставка новых маркеров CYP2C9_2 и CYP2C9_3**


### 2. Бэкенд: сущность PatientGeneticResult
- Удалить свойства `testDate`, `comment` и их геттеры/сеттеры.
- Удалить соответствующие аннотации/атрибуты ORM (но колонок уже нет, можно просто закомментировать или удалить).
- Убедиться, что в группах сериализации нет `test_date` и `comment` (на всякий случай).

### 3. Бэкенд: сервис PharmacogeneticsService
- Метод `getPatientPharmacogenetics(int $patientId, ?int $drugId = null)`.
- Если `$drugId` передан, выбирать маркеры только те, что связаны с препаратом через `marker_drug_relations`.
- Исключить из возвращаемых DTO поля `testDate` и `comment` (они уже не возвращаются, но проверить).
- Убедиться, что для варфарина возвращаются ровно четыре маркера: CYP2C9_2, CYP2C9_3, VKORC1_3673, VKORC1_3730.

### 4. Бэкенд: PharmacogeneticsProvider
- Добавить поддержку query-параметра `drug` (IRI препарата).
- Извлечь ID препарата из IRI, передать в сервис.
- Вернуть ответ без лишних полей.

### 5. Бэкенд: процессор PatientGeneticResultProcessor
- Убедиться, что при создании/обновлении результат принимает только `patient`, `marker`, `markerValue` (без `testDate`, `comment`).
- Логика сравнения и отправки запросов остаётся без изменений.

### 6. Бэкенд: сидер app:seed-genetic-markers
- Удалить создание старого CYP2C9 (диплотип).
- Добавить создание CYP2C9_2 и CYP2C9_3 с возможными значениями и связями с варфарином/фенилином.
- Обеспечить идемпотентность (проверять существование маркеров перед вставкой).

### 7. Фронтенд: компонент Pharmacogenetics
- Принимать новый prop `drugIri` (или `drugId`).
- Передавать его в API-запрос `getForPatient(this.patientId, { params: { drug: this.drugIri } })`.
- Удалить из модели маркера поля `editingTestDate`, `editingComment`, а также связанный код их обработки.
- В шаблоне редактирования убрать `<input type="date">` и `<textarea>`.
- В шаблоне просмотра убрать отображение даты теста (и иконку комментария, если она была).
- Логика сохранения: payload только с `patient`, `marker`, `markerValue` (без даты/комментария).

### 8. Фронтенд: родительский компонент PatientHistory
- Передавать в `<Pharmacogenetics>` параметр `drug-iri` из `treatmentStore.treatment.drug` (если есть).
- Убедиться, что блок фармакогенетики корректно обновляется при смене препарата (добавить watch на `drugIri` в Pharmacogenetics).

### 9. Тестирование
- Проверить через Swagger/CURL, что `GET /api/patients/{id}/pharmacogenetics?drug=/api/drugs/X` возвращает нужные маркеры, без test_date/comment.
- Проверить создание/обновление/удаление результатов через API.
- Проверить интерфейс: на пациенте с варфарином видны 4 выпадающих списка, нет полей даты/комментария.
- Проверить, что при выборе «Не исследовано» происходит удаление.
- Проверить пациентов с другими препаратами.
- Запустить сидер дважды, убедиться в идемпотентности.


### Код


### Дневник


## Тестирование

