### Описание

Отлично, Максим. Учтём принцип: что не используется за пределами модуля — остаётся внутри него. Разработчик сам определит границы `shared`, ориентируясь на реальное использование. Даю финальную постановку.

---

**Задача 2.26. Рефакторинг фронтенда: переход на Feature-based архитектуру с модулем patientManagement**

- **Спринт:** 2  
- **Код:** 2.26  
- **Оценка:** 1.5 дня  
- **Функция:** FUNC-029 (Инфраструктура, рефакторинг)  
- **Статус:** К выполнению  

**Цель:**  
Перестроить структуру фронтенда, избавившись от модуля `physicianDashboard`, и организовать код по функциональным зонам интерфейса врача. Выделить модули `patientManagement` (списки пациентов и добавление сущностей), `patientCard` (карточка пациента), `shared` (общие компоненты, layout, сторы). Обеспечить чёткое разделение ответственности и подготовить архитектуру для новых задач (2.22 и далее).

**Исходное состояние:**  
- Часть компонентов (списки) уже лежит в `modules/physicianDashboard`.  
- Карточка пациента и связанные компоненты — в `components/PatientHistory/`.  
- Сторы разбросаны: `stores/` и `modules/physicianDashboard/stores/`.  
- Общие элементы интерфейса (Sidebar, MainHeader, PatientAdd, TreatmentAdd) — в `components/`.  
- API-клиенты (`src/api/`) остаются на верхнем уровне и не трогаются.

**Требования:**

1. **Целевая структура модулей (концептуально):**

```
modules/
├── patientManagement/          # Списки пациентов, добавление пациентов и лечений
│   ├── components/
│   │   ├── PatientMonitoring/
│   │   ├── PatientWorkList/
│   │   ├── PatientListPanel/
│   │   ├── PatientAdd/
│   │   ├── TreatmentAdd/
│   │   └── PatientTable/       # используется только здесь
│   ├── stores/
│   │   ├── monitoringStore.js
│   │   ├── workListStore.js
│   │   └── patientListStore.js (если потребуется)
│   └── composables/
│       └── useDrugTabs.js
├── patientCard/                # Карточка пациента (история, лечение, показатели)
│   ├── components/
│   │   ├── PatientHistory/
│   │   ├── PatientCard/
│   │   ├── TreatmentCard/
│   │   ├── Pharmacogenetics/
│   │   ├── MedicalTable/
│   │   ├── AppointmentAdd/
│   │   ├── TestAddModal/
│   │   └── MnoChart/
│   │   (VitalsCard/ появится позже)
│   └── stores/
│       ├── patientCardStore.js
│       ├── treatmentStore.js
│       ├── pharmacogeneticsStore.js
│       └── medicalTableStore.js
├── shared/                     # Только действительно общие элементы
│   ├── components/
│   │   ├── MultiDiagnosisSelect/
│   │   └── ... (другие переиспользуемые UI-компоненты)
│   ├── layout/
│   │   ├── Sidebar/
│   │   └── MainHeader.vue
│   ├── composables/
│   │   ├── usePagination.js
│   │   └── ... (будущие общие composables)
│   └── stores/
│       ├── patientStore.js
│       ├── hospitalStore.js
│       ├── authStore.js
│       └── drugGroupStore.js
```

- Принцип размещения: если компонент/стор используется **только** в одном модуле — он остаётся внутри этого модуля. Если используется в двух и более — выносится в `shared`. Разработчик при рефакторинге принимает окончательное решение по каждому элементу, руководствуясь этим принципом.

2. **Основные перемещения (таблица миграции):**

| Исходное расположение | Целевое расположение |
|----------------------|----------------------|
| `modules/physicianDashboard/components/PatientMonitoring` | `modules/patientManagement/components/PatientMonitoring` |
| `modules/physicianDashboard/components/PatientWorkList` | `modules/patientManagement/components/PatientWorkList` |
| `modules/physicianDashboard/components/PatientTable` | `modules/patientManagement/components/PatientTable` (не в shared!) |
| `modules/physicianDashboard/stores/monitoringStore.js` | `modules/patientManagement/stores/monitoringStore.js` |
| `modules/physicianDashboard/stores/workListStore.js` | `modules/patientManagement/stores/workListStore.js` |
| `modules/physicianDashboard/composables/useDrugTabs.js` | `modules/patientManagement/composables/useDrugTabs.js` |
| `modules/physicianDashboard/composables/usePagination.js` | `modules/shared/composables/usePagination.js` (если используется вне patientManagement) |
| `components/PatientListPanel/*` | `modules/patientManagement/components/PatientListPanel/*` |
| `components/PatientAdd/*` | `modules/patientManagement/components/PatientAdd/*` |
| `components/TreatmentAdd/*` | `modules/patientManagement/components/TreatmentAdd/*` |
| `components/PatientHistory/*` (вся папка) | `modules/patientCard/components/PatientHistory/*` |
| `components/PatientHistory/PatientCard` | Оставить внутри `PatientHistory` или вынести в `modules/patientCard/components/PatientCard` (на усмотрение разработчика) |
| `components/PatientHistory/TreatmentCard` | Аналогично — внутри `patientCard` |
| `components/PatientHistory/Pharmacogenetics` | Аналогично |
| `components/PatientHistory/MedicalTable` | Аналогично |
| `components/PatientHistory/AppointmentAdd` | Аналогично |
| `components/PatientHistory/TestAddModal` | Аналогично |
| `components/PatientHistory/MnoChart` | Аналогично |
| `stores/patientCardStore.js` | `modules/patientCard/stores/patientCardStore.js` |
| `stores/treatmentStore.js` | `modules/patientCard/stores/treatmentStore.js` |
| `stores/pharmacogeneticsStore.js` | `modules/patientCard/stores/pharmacogeneticsStore.js` |
| `stores/medicalTableStore.js` | `modules/patientCard/stores/medicalTableStore.js` |
| `stores/patientStore.js` | `modules/shared/stores/patientStore.js` (если используется в нескольких модулях) |
| `stores/hospitalStore.js` | `modules/shared/stores/hospitalStore.js` |
| `stores/authStore.js` | `modules/shared/stores/authStore.js` |
| `stores/drugGroupStore.js` | `modules/shared/stores/drugGroupStore.js` |
| `components/Sidebar/*` | `modules/shared/layout/Sidebar/*` |
| `components/MainHeader.vue` | `modules/shared/layout/MainHeader.vue` |
| `components/common/MultiDiagnosisSelect` | `modules/shared/components/MultiDiagnosisSelect` |

После переноса **удалить** пустую папку `modules/physicianDashboard` и очистить `components/` от перемещённых папок (кроме `Login` и того, что решено оставить).

3. **Импорты и роутинг:**
   - Исправить все импорты в перемещённых файлах и файлах, которые на них ссылаются.
   - Обновить `router/index.js`, указав новые пути к страницам.
   - Убедиться, что проект собирается без ошибок (`npm run build` проходит).

4. **Проверка:**
   - Запустить dev-сервер, открыть домашнюю страницу (список пациентов), карточку пациента, форму добавления пациента. Все страницы должны работать идентично состоянию до рефакторинга.
   - В консоли браузера не должно быть новых ошибок.
   - Проверить функциональность: загрузка списков, переключение вкладок препаратов, открытие модальных окон, сохранение данных.

**Ожидаемый результат:**
- Фронтенд организован по feature-based принципу, `physicianDashboard` полностью удалён.
- `patientManagement` содержит всё для работы со списками и добавления пациентов/лечений.
- `patientCard` инкапсулирует всю логику карточки пациента.
- `shared` содержит только переиспользуемые элементы, без привязки к конкретному модулю.
- Новая функциональность (задача 2.22) может быть сразу добавлена в `patientCard` без дополнительных перестроек.

**Зависимости:**  
- Не зависит от других задач, может выполняться в любой момент.  
- Рекомендуется завершить до начала задачи 2.22, чтобы `VitalsCard` и `vitalsStore` создавались сразу в правильных модулях.

### Ход разработки

### Тестирование

