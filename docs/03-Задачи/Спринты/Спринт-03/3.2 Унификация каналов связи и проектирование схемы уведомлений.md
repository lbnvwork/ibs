### Описание

### Ход разработки

#### Ревью

api/migrations/Version20260806124610.php

1. нет поля body
2. нет правила ON DELETE SET NULL

api/src/Ibs/Context/Communication/Entity/NotificationLog.php

1. Непонятно, зачем добавлен #[ApiResource] к NotificationLog
2. Заменить строки `channelType`, `priority`, `status` на **Enum**
3. Добавить:  Составной индекс `(patient_id, created_at DESC)`

api/src/Ibs/Context/Communication/Entity/NotificationTemplate.php
1. Добавить **Enum `ChannelType`**
2. **Добавить валидацию** — `Assert\NotBlank` для `code` и `channel`, `Assert\Length`

api/src/Ibs/Context/Communication/Model/NotificationMessage.php
1. **Добавить валидацию** — `Assert\NotBlank` на `body`, `Assert\Length` на `template`

api/src/Ibs/Context/Communication/Model/Recipient.php
1. **Типизировать `channelType`** : Enum `ChannelType`
2. **Добавить валидацию** — например, `Assert\AtLeastOneOf` для контактных данных
3. **Для логов** можно сохранять не только адрес, но и тип канала, что уже делается в `NotificationLog`.

api/src/Ibs/Context/Communication/Repository/NotificationLogRepository.php
1. Добавить пагинацию для истории (например, метод `findByPatientPaginated` с `Query` и `Paginator`)
2. Добавить метод для выборки по статусу (например, «показать только неудачные»)
3. Типизировать возвращаемые массивы как `array<int, NotificationLog>`

api/src/Ibs/Context/Communication/Repository/NotificationTemplateRepository.php
1. Добавить `findByChannel(string $channel): array` для получения всех шаблонов определённого канала.
2. добавить кэширование (например, через `Symfony\Contracts\Cache\CacheInterface`)

api/src/Ibs/Context/Communication/Service/NotificationService.php
1. **Enum для статусов** — `'sent'`, `'failed'` лучше заменить на `enum Status: string`
2. **Асинхронное логирование** — при больших объёмах можно вынести логирование в отдельный обработчик, чтобы не замедлять основную отправку.
3. **Кэширование шаблонов** — если шаблоны редко меняются, `TemplateResolver` можно обернуть в кэш, чтобы не ходить в БД при каждой отправке.

api/src/Ibs/Context/Communication/Service/TemplateResolver.php
1. **Валидация** — проверять, что после подстановки текст не стал пустым.
2. **Типизация данных** — сейчас `$data: array<string, scalar|null>`,  можно заменить на более строгий контракт.

api/tests/Ibs/Context/Communication/Entity/CommunicationPersistenceTest.php
1. Добавить тест **Граничные значения для полей**
   - `channelType` максимальной длины (32 символа), `priority` (16 символов), `templateCode` (64 символа).
- `bodyTemplate` с очень длинным текстом (например, 10 000 символов) — убедиться, что `text` тип не обрезает.
- `patientId` = `null` (уведомление без привязки к пациенту) — проверить, что сохраняется.
2. **Уникальность шаблонов**
   Создать два шаблона с одинаковыми `code` и `channel` → ожидать исключение `UniqueConstraintViolationException`.
3. **Связь логов с историей (опционально)**
   Сохранить несколько логов для одного пациента, вызвать `NotificationLogRepository::findByPatient()` и проверить, что все они возвращаются.
4. **Enum Priority**
   Сейчас `priority` сохраняется как строка. Можно протестировать, что сохранение с `Priority::ROUTINE->value` и последующая загрузка дают тот же `'routine'`.
5. **Дата создания**
   Проверить, что `createdAt` сохраняется с переданным значением, а не перезаписывается конструктором (у нас конструктора нет, так что тест уже это делает).

api/tests/Ibs/Context/Communication/Model/RecipientTest.php

1. **Проверка неизменяемости** — убедиться, что после создания `Recipient` нельзя изменить поля (они `readonly`).
2. **Проверка пустого конструктора** — создать `Recipient()` и убедиться, что все поля `null`.
3. **Проверка, что `maxUserId` не путается с другими полями** (уже косвенно покрыто).

api/tests/Ibs/Context/Communication/Model/SendResultTest.php
1. **Проверка неизменяемости** — убедиться, что после создания `SendResult` его поля `readonly` и не могут быть изменены (это уже обеспечивается самим PHP).
2. **Проверка `failure()` с пустой строкой** — можно решить, что ошибка обязана быть непустой, и добавить проверку `assertNotEmpty` при необходимости.

### Тестирование

